---
title: ปรับโค้ดรับ schema ฐานข้อมูลใหม่ 17 คอลัมน์ + กติกาแสดงชื่อล่าสุด — Forge
file_type: output_dev
agent_owner: Forge
last_updated: 2026-07-16
---

# ปรับโค้ดรับ schema ใหม่ (Jed ปรับ Sheet เอง 16 ก.ค.69) — Forge

## โครงสร้างใหม่ Sheet "ระบบสมาชิก ชศพอ." (ยืนยันจาก Sheet จริงผ่าน Drive)
รุ่นที่(0) ลำดับ(1) เลขที่(2) ยศ(3) ชื่อแรก(4) ชื่อ2(5) ชื่อ3(6) นามสกุลแรก(7) นามสกุล2(8) นามสกุล3(9)
ประเภท(10) สถานะ(11) สถานที่ปฏิบัติงาน(12) สถาบัน(13) ที่อยู่(14) เบอร์โทร(15) หมายเหตุ(16)
- ฐานรวมทุกคน ~3,700 แถว รวม "ไม่เป็นสมาชิก" (เลขที่ว่าง)
- คอลัมน์ที่หายไป: IMAGE, วันเกิด, เบอร์ทำงาน/บ้าน, สถานภาพสมรส, คู่สมรส

## กติกาแสดงชื่อ (คำสั่ง Jed)
ใช้ชื่อล่าสุด (ชื่อ3 → ชื่อ2 → ชื่อแรก) ถ้าเคยเปลี่ยนต่อท้าย "(ชื่อแรก)" — นามสกุลกติกาเดียวกัน
เช่น "สมใจ (สมหญิง) ทองคำ (ทองดี)" — helper `displayName()` ใน netlify-deploy/js/ui.js

## สิ่งที่แก้
- **apps-script-new/รหัส.js**: COL ใหม่ทั้งชุด, rowToMember ส่ง fname1-3/lname1-3/note, ค้นหาครอบคลุมชื่อทุกช่วง,
  แก้/ลบสมาชิกอ้างอิง `row_num` (เลขแถวจริง) + `expect_fname1` กัน cache เลื่อนแล้วลบผิดคน,
  addMember คำนวณลำดับในรุ่นอัตโนมัติ, exportMembers mirror sheet 17 คอลัมน์,
  getStats เพิ่ม `members` (สามัญ+สมทบ), ลบ setupOldNameColumns (ไม่ต้องรันอีกแล้ว), approveMember (dormant) map ใหม่
- **frontend (netlify-deploy/)**: ui.js เพิ่ม displayName/memberDisplayFname/Lname,
  members.js + admin.js ใช้ชื่อล่าสุด + ค้นหา 6 ช่องชื่อ + cache key bump เป็น members_data_v2,
  admin/index.html รื้อ modal แก้ไข/เพิ่มสมาชิกเป็นฟิลด์ชุดใหม่ (เลขที่แก้ได้, ตัดวันเกิด/สมรส ฯลฯ),
  home.js สถิติหน้าแรกนับเฉพาะสามัญ+สมทบ (แก้บั๊กเดิม stat "สมาชิกสามัญ" ที่โชว์ยอด "มีชีวิต")
- CLAUDE.md อัปเดต schema

## Verify แล้ว (local preview + mock)
members.html + admin panel render ถูกตามกติกาทุกเคส (ไม่เปลี่ยน/เปลี่ยน 1 ครั้ง/เปลี่ยน 2 ครั้ง), modal ครบ, ไม่มี JS error, node --check ผ่าน

## ⚠️ ตอนนี้เว็บ live อ่านคอลัมน์เพี้ยนอยู่ (GAS เก่าอ่าน Sheet ใหม่) — Jed ต้อง deploy:
1. เปิด Apps Script (บัญชี nurse.rtafnc) → paste `apps-script-new/รหัส.js` ทับทั้งไฟล์ → Deploy → Manage deployments → ✏️ → New version
2. ลากโฟลเดอร์ `netlify-deploy/` ขึ้น Netlify ทับอีกรอบ
3. หมายเหตุ: ไม่ต้องรัน setupOldNameColumns แล้ว

## รอบแก้เพิ่ม (16 ก.ค. เย็น — คำสั่ง Jed)
1. ชื่อ-นามสกุลเดิมในวงเล็บเป็นสีเทาอ่อน (#9ca3af, class `.old-name` ใน css/style.css + `displayNameHtml()` ใน ui.js) — ใช้ทั้งหน้า public และ admin
2. register.html: หัวรองตัด "(ตลอดชีพ)", กล่องเหลือง → "ส่งใบสมัครผ่าน ตัวแทนรุ่น ของแต่ละรุ่นเท่านั้น",
   ปุ่มโหลด → PDF `docs/ใบสมัคร_ชศพอ.pdf` (ทดสอบ 200/application/pdf), นิยามสามัญ/สมทบใหม่ (วพอ. / พยาบาลศาสตรบัณฑิตสถาบันอื่น+สายวิทยาการเหล่าทหารแพทย์),
   ข้อ 3 ตัด "ตลอดชีพ" → "ชำระเงินค่าสมัคร 1,000 บาท ผ่านทางตัวแทนรุ่นของท่าน"
   หมายเหตุ: แบนเนอร์ deadline 30 ก.ย.69 ถูกแทนที่ด้วยข้อความตัวแทนรุ่นตามสั่ง — วันปิดรับไม่แสดงบนหน้าแล้ว

## รอบแก้เพิ่ม 2 (16 ก.ค. ค่ำ) — ฟอร์ม Admin "+เพิ่มสมาชิก" ตามใบสมัครฉบับปรับปรุง ก.ค.2569
- ฟิลด์เรียงตามกระดาษ: คำนำหน้า, ชื่อ(ปัจจุบัน)*, ชื่อเดิม(แรก), ชื่อเดิม(2), นามสกุล(ปัจจุบัน)*, นามสกุล(แรก), นามสกุล(2),
  ปี/รุ่นที่จบ*, สถาบันที่จบ, สถานที่ปฏิบัติงาน, ที่อยู่, เบอร์โทร, ประเภท (สามัญ/สมทบ) + ส่วน "สำหรับเจ้าหน้าที่" (เลขที่, สถานะ, หมายเหตุ)
- `chronoNames()` ใน admin.js แปลงมุมมองใบสมัคร (ปัจจุบัน/เดิมแรก/เดิม2) → ลำดับเวลาใน DB (ชื่อแรก/ชื่อ2/ชื่อ3=ล่าสุด) อัตโนมัติ
- verify แล้ว: เปลี่ยนชื่อ 2 ครั้ง → fname1=เดิมแรก, fname2=เดิม2, fname3=ปัจจุบัน / ไม่เคยเปลี่ยน → ลงช่องแรกช่องเดียว

## ประเด็นค้างให้ Jed ตัดสิน/เก็บกวาด
- หัวคอลัมน์ B ใน Sheet เขียนว่า "รุ่นที่" ซ้ำกับ A — ควรแก้เป็น "ลำดับ"
- บางแถวใน Sheet merge เซลล์ ที่อยู่+เบอร์โทร เป็นช่องเดียว — ควร unmerge ไม่งั้นเบอร์โทรอ่านไม่ออก
- หน้า public "ค้นหาสมาชิก" ตอนนี้แสดงคน "ไม่เป็นสมาชิก" ด้วย (กรองด้วย dropdown ประเภทได้) — ถ้าอยากซ่อน บอก Forge เพิ่ม filter ได้
